Если символ типа не встретился, а встретился другой - ничего не выводится и поход по строке начинается с симовла, который встретился вместо символа типа.
Если просто точка без чисел после нее, то что?
Если precision = -1 - число округляется и выводится без точности, как интовое

modifier_h = 1;
modifier_hh = 2;
modifier_l = 3;
modifier_ll = 4;
modifier_L = 5;

Проверка на считывание

printf("\nminus = %d, plus = %d, space = %d, zero = %d, hash = %d\n",
		all.flag_minus, all.flag_plus, all.flag_space, all.flag_zero, all.flag_hash);
printf("width = %d, precision = %d\n", all.width, all.precision);
printf("modifier = %d\n", all.modifier);

1 - d;
2 - i;
3 - o;
4 - u;
5 - x;
6 - X;
